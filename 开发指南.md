# 海康互联固件升级工具 - 开发指南

## 1. 工程结构概览

以 `hikiot` 目录为核心：

- `hikiot/firmware_upgrade_main.py`  
  整个工具的主入口，负责初始化日志、路径、UI、控制器。

- `hikiot/view/firmware_upgrade_application.py`  
  使用 `pygubu` + `firmware_upgrade.ui` 构建 Tk UI。  
  暴露 `global_firmware_upgrade_app`，并提供 `initialize_ui()` / `run()` / `close()`。

- `hikiot/view/firmware_upgrade.ui`  
  UI 布局文件（XML），由 pygubu 生成/维护。

- `hikiot/controller/hikiot_open_firmware_upgrade_controller.py`  
  核心业务逻辑：加载 Excel、批量添加设备、下发升级任务、轮次管理、导出报表等。  
  暴露 `global_firmware_upgrade_controller`。

- `hikiot/utils/`  
  - `log_manager.py`：日志系统（控制台 + 文件 + UI）。  
  - `thread_manager.py`：统一线程池与任务调度。  
  - `path_manager.py`：应用目录、日志目录、数据目录路径管理。  
  - `excel_processor.py`：Excel 读取与预处理。  
  - `http_client.py` / `firmware_http_server.py` 等：HTTP 请求与本地固件服务（视具体使用情况）。

- `hikiot/model/`  
  - `hikiot_open_auth_app_access_token.py`：获取 AppAccessToken。  
  - `hikiot_open_auth_user_access_token.py`：获取 UserAccessToken。  
  - `hikiot_open_device_management.py`：设备添加、删除等。  
  - `hikiot_open_get_device_info.py`：查询设备状态与信息。  
  - `hikiot_open_firmware_upgrade.py`：创建升级任务、查询进度。

- `hikiot/config/default_credentials.json`  
  默认账号配置（AppKey、AppSecret、用户名、密码），启动时自动填入 UI。

- `hikiot/docs/`  
  BUG 与线程模型分析文档、后续可以追加更多设计文档。

## 2. 启动方式与运行环境

### 2.1 启动方式

在工程根目录（包含 `hikiot/` 的目录）执行：

```bash
# 推荐：模块方式
python -m hikiot.firmware_upgrade_main

# 或者：直接指定脚本
python hikiot\firmware_upgrade_main.py
```

### 2.2 依赖与虚拟环境

建议使用虚拟环境并在根目录维护 `requirements.txt`，典型依赖包括（仅示例）：

- `pygubu`
- `pandas`
- `openpyxl`
- `requests`
- `pytz`

安装方式示例：

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

## 3. 关键模块与设计要点

### 3.1 日志系统（`hikiot/utils/log_manager.py`）

- 使用 `LogManager` 单例，暴露 `global_logger`。  
- 日志输出：
  - 控制台：`StreamHandler`。  
  - 文件：`logs/application_YYYYMMDD.log`。  
  - UI：`UIHandler`，通过队列 + `root.after()` 在主线程更新 Text 控件。
- 日志格式：`[时间][调用文件:行号][级别]消息`。  
- 线程安全：
  - 工作线程只 `emit()` 到队列，UI 更新在主线程轮询 `_drain_log_queue()`。

### 3.2 线程与任务（`thread_manager` + 控制器）

- 使用统一线程池（`global_thread_manager`），封装任务提交与优先级。  
- 控制器通过：
  - `global_thread_manager.submit_task(...)` 提交后台工作。  
  - 所有 UI 更新必须通过 `HikiotFirmwareUpgradeController._safe_after()` 调度到主线程。

### 3.3 控制器核心（`HikiotFirmwareUpgradeController`）

主要职责：

- **加载 Excel 设备列表**
  - 从 Excel 读取 `serial` / `验证码` / `firmware1` / `firmware2`。  
  - 构建 `self.device_firmware_map[serial] = {'firmware1': ..., 'firmware2': ...}`。  
  - 将数据写入 Treeview 行，URL 只存入隐藏列，不在 UI 显示。

- **批量添加设备**
  - 使用 `global_device_management_model.add_device`。  
  - UI 状态变化全部走 `_safe_after`。  
  - 添加成功/已存在的设备，会调用 `_fetch_and_update_device_info()` 获取设备名、版本、在线状态并更新 UI。

- **升级流程**
  - 每台设备对应一个 `DeviceUpgradeWorker`，独立线程执行多轮升级。  
  - 每轮执行：
    - 自动获取 AppToken / UserToken。  
    - 按设备级配置与轮次选择固件（支持 firmware1 / firmware2）。  
    - 创建升级任务，进入进度轮询。  
    - 专门处理失败状态、超时（15 分钟）等。  
    - 轮次之间有重启等待和设备上线检测逻辑。  
  - 所有 UI 更新（状态、进度、轮次、成功/失败次数、设备版本）都通过 `_update_ui_xxx()` + `_safe_after()` 实现。

- **导出报表与日志**
  - 升级全部完成后触发 `_export_upgrade_results_to_excel()`：
    - 生成 `data/report/upgrade_result_时间戳.xlsx`。  
    - Sheet1：`升级摘要`，所有设备的汇总信息。  
    - 后续多个 Sheet：每台设备一个子表，记录该设备每轮的详细信息（含升级前/后版本、错误信息、超时标记等）。
  - 额外复制本次升级相关日志：
    - 路径：`data/upgrade_logs_时间戳/<serial>/round_XX.log`。  
    - 按设备 + 轮次 + 时间窗口从主日志中筛选相关行，方便排查。

### 3.4 `DeviceUpgradeWorker`

- 每个设备一个实例，负责完整多轮升级。  
- 关键字段：
  - `round_records: List[UpgradeRoundRecord]`：记录每轮开始/结束时间、结果、错误状态/信息、是否超时、超时时间段、升级前/后版本等。

设计要点：

- 所有耗时逻辑（sleep、API 调用）都在工作线程执行。  
- 所有 UI 更新通过控制器 `_safe_after` 跳回主线程。  
- `stop_flag` 供控制器在“停止升级”时通知 worker 尽快退出。

## 4. 开发规范与注意事项

1. **禁止在非主线程直接操作 Tkinter**  
   - 只能在主线程里操作 UI 控件；后台线程必须使用 `_safe_after()` 或 `UIHandler` 的队列。

2. **增加列/字段时保持索引一致**  
   - Treeview 的列顺序与控制器中对 `values[i]` 的访问需要同步维护。

3. **导出逻辑只做“附加”不影响流程**  
   - 报表导出与日志复制应尽量不影响主升级流程，出现异常时只记录日志，不影响 UI 关闭。

4. **多线程下的共享状态访问加锁**  
   - 如 `completed_devices_count` 使用 `completed_lock` 保护。

5. **新增 API 调用建议封装到 model 层**  
   - 避免在控制器中直接写一大堆 HTTP 请求细节，尽量通过 `hikiot/model` 里的封装类来调用。

## 5. 扩展方向建议

- 增加更多过滤/筛选条件（按设备名/状态过滤视图）。  
- 支持从 API 拉取设备列表，而不仅仅是从 Excel 导入。  
- 增加“重试失败轮次”功能（对部分失败设备再次下发任务）。

