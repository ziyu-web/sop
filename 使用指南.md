# 海康互联固件升级工具 - 使用指南

## 1. 启动工具

### 1.1 环境准备

1. 安装 Python 3（建议 3.9+）。  
2. 在工程根目录（包含 `hikiot` 目录的那一层）创建并激活虚拟环境（可选但推荐）：

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt    # 如果有这个文件
```

### 1.2 启动方式

在 **工程根目录** 执行：

```bash
# 推荐方式
python -m hikiot.firmware_upgrade_main

# 或者
python hikiot\firmware_upgrade_main.py
```

启动后会弹出“海康互联开放平台 - 固件升级压力测试工具”窗口。

## 2. 准备 Excel 设备列表

Excel 中每一行代表一台设备，至少需要以下列（从左到右）：

1. **A 列：设备序列号（必填）**
2. **B 列：验证码（必填，用于添加设备）**
3. **C 列：升级包 1（firmware1，可为 URL 或本地路径）**
4. **D 列：升级包 2（firmware2，可选，用于偶数轮）**

> 说明：  
> - 如果使用 URL，支持 `http://` 或 `https://`；  
> - 如果使用本地路径，请保证在运行机器上可访问。

## 3. 界面说明

主窗口大致分为三块：

1. **顶部配置区**
   - AppKey / AppSecret / 用户名 / 密码  
   - 设备列表文件选择与“加载”按钮  
   - 升级轮次数（循环次数）  
   - 状态栏（显示当前总体状态）

2. **中间设备列表（Treeview）**
   每行代表一台设备，主要列：

   - 选择（☐ / ☑）  
   - 设备序列号  
   - 验证码  
   - 设备名称（自动从平台查询）  
   - 当前版本  
   - 设备状态（在线 / 离线）  
   - 当前轮次（已完成轮数 / 总轮次）  
   - 成功/失败（成功次数 / 失败次数）  
   - 升级状态（创建中、upgrading、completed、失败、TIMEOUT 等）  
   - 进度（0% ~ 100%）  
   - 消息（错误信息等）

3. **右侧日志输出**
   - 实时显示关键日志（INFO/WARNING/ERROR 等），颜色区分级别。  
   - 支持滚动查看历史信息。

## 4. 基本操作流程

### 4.1 配置账号信息

1. 在顶部输入：
   - AppKey  
   - AppSecret  
   - 用户名  
   - 密码  
2. 如已配置 `hikiot/config/default_credentials.json`，启动时会自动填充。

### 4.2 加载设备列表

1. 点击“设备列表文件”右侧的“浏览/加载”按钮。  
2. 选择准备好的 Excel 文件。  
3. 成功后：
   - 设备列表会显示所有设备的序列号和验证码。  
   - 列中**不会**显示固件 URL，仅在内部隐藏列中保存。

### 4.3 批量添加设备

1. 确保账号信息有效。  
2. 点击“批量添加设备”按钮：
   - 工具会依次调用平台接口，把设备添加到团队。  
   - UI 中“状态”与“消息”列会显示“已添加 / 已存在 / 失败”等结果。  
   - 对于“已添加”或“已存在”的设备，工具会自动调用 `get_device_info`：
     - 填充“设备名称”列。  
     - 填充“当前版本”列。  
     - 填充“设备状态”（在线/离线）。

### 4.4 设置升级轮次

- 在“循环次数”输入框中设置要升级的轮次数（默认至少 1）。  
- 若 Excel 中同时配置了 C 列（firmware1）和 D 列（firmware2）：
  - 奇数轮默认使用 firmware1；  
  - 偶数轮默认使用 firmware2（如果该设备配置了）。

### 4.5 选择设备并开始升级

1. 在设备列表中勾选需要升级的设备（☑）。  
   - 可通过右键菜单或“全选”功能快速选择。  
2. 点击“开始升级”：
   - 每台设备会启动一个独立的后台任务（线程），按指定轮次进行升级。  
   - “当前轮次 / 成功/失败 / 升级状态 / 进度 / 消息”会实时更新。  
   - 升级前工具会先查询并显示“升级前版本”。

### 4.6 停止升级

- 点击“停止升级”按钮：
  - 会向所有设备任务发送停止信号；  
  - 正在运行的轮次会尽快结束，工具停止继续下发新的轮次。

## 5. 升级结果与日志

### 5.1 报表导出（Excel）

当所有选中设备的所有轮次完成后，工具会自动导出报表到：

```text
data/report/upgrade_result_YYYYMMDD_HHMMSS.xlsx
```

结构：

- `升级摘要` 子表：
  - 每行一台设备：设备序列号、验证码、总轮次、成功次数、失败次数、成功率。

- 多个“设备子表”：
  - 每台设备一个 sheet（以序列号命名，超长会截断）。  
  - 每行一轮：轮次、结果（成功/失败）、升级前版本、升级后版本、开始/结束时间、错误状态、错误消息、是否超时、超时时间段。

> 说明：如果某轮升级失败或设备未重新上线，“升级后版本”可能为空。

### 5.2 本次升级的日志副本

工具会把**当前应用日志文件**按设备与轮次切分一份副本，存放在：

```text
data/upgrade_logs_YYYYMMDD_HHMMSS/<设备序列号>/round_XX.log
```

- 每个 `round_XX.log` 尝试只保留该设备该轮时间区间内、且包含该设备序列号的日志行。  
- 便于定位单台设备在某一轮中的所有日志。

### 5.3 主日志文件

主日志文件位于：

```text
logs/application_YYYYMMDD.log
```

记录了工具启动、HTTP 调用、升级流程等所有关键信息。

## 6. 常见问题

### 6.1 启动时报 `ModuleNotFoundError: hikiot`

- 一般是因为在 `hikiot` 子目录里直接用模块方式运行。  
- 请确认 **当前目录是工程根目录**，然后执行：

```bash
python -m hikiot.firmware_upgrade_main
```

或：

```bash
python hikiot\firmware_upgrade_main.py
```

### 6.2 UI 没有响应 / 卡死

- 确保不要在你自己添加的代码里从工作线程直接操作 Tk 控件。  
- 所有 UI 更新请交给控制器的 `_safe_after()` 来做。

### 6.3 报表里某些轮次没有“升级后版本”

- 这种情况一般为：
  - 升级任务没成功；  
  - 或设备重启/上线检测超时；  
  - 工具只在确认设备重新上线时更新“升级后版本”字段，未上线则留空。

